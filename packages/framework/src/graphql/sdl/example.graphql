extend type Query{
    find(model:String!,filter:JSON):JSON @find
}
extend type Mutation{
    insert(model:String!,data:JSON!):JSON @insert
    updateById(model:String!,data:JSON!):JSON @updateById
}

