import React from 'react'

export default () => {
    // const reloadFind = useState(0);
    // console.log('reloadFind::', reloadFind)
    return (
        <div>
            Im Form composed with a gql and selected name of input
       </div>
    )
}