export default () => {
    // export default async ({ db, tableName, _id }) => {
    // try {
    //     let response = await db(tableName)
    //         .where({ _id })
    //         .del()
    //     return response === 1 ?
    //         {
    //             error: null,
    //             data: 'Correct Deleted'
    //         } : {
    //             error: `Error:${response}. Cannot be deleted the _id:${_id}`,
    //             data: null
    //         }
    // } catch (error) {
    //     return {
    //         error: 'Error.' + error,
    //         data: null
    //     }
    // }

}