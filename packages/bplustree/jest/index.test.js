import btree from '../src'
import with3 from './with3'
import with4 from './with4'
import with5 from './with5'
import with9 from './with9'
// with3()
// with4()
// with5()
with9()

// describe('test with 3 inputs', () => {
//     const tree = btree({})
//     tree.put(10, 'hola')
//     tree.put(1, 'hola')
//     tree.put(2, 'hola')
//     test('with 3 inputs', () => {
//         // console.log('tree.getTree.noneLeafs[0].block[0]::', tree.getTree.noneLeafs[0].blocks[0])
//         //noneleaf total
//         expect(
//             Object.entries(tree.getTree.noneLeafs).length
//         ).toBe(1)

//         expect(
//             tree.getTree.noneLeafs[0].blocks.length
//         ).toBe(1)

//         expect(
//             tree.getTree.noneLeafs[0].blocks[0].key
//         ).toBe(2)

//         expect(
//             Object.entries(tree.getTree.leafs).length
//         ).toBe(2)
//     })


// })
